version: 2
updates:
  # GitHub Actions versions are managed manually — Dependabot repeatedly proposes
  # non-existent major versions (e.g. checkout@v6, setup-node@v6).
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
